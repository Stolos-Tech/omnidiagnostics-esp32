# esp32-os custom partition table for 4MB flash.
# WiFi stack + Berry + Bluetooth Classic do not fit the default 1.3MB app.
# No OTA -> single large app (~2.75MB) + LittleFS (~1.1MB).
# After changing this table you MUST reflash both app (upload) AND filesystem
# (uploadfs): the spiffs offset changed, old data at the old offset is invalid.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x2C0000,
spiffs,   data, spiffs,  0x2D0000, 0x120000,
coredump, data, coredump,0x3F0000, 0x10000,
