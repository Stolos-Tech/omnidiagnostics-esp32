// Евристика виявлення Bluetooth card-скімерів за іменем пристрою (чиста логіка).
// Скімери карток масово будують на дешевих SPP-модулях (HC-05/06, linvor, JDY,
// BT04...) з дефолтними іменами. Збіг = ПІДОЗРА (не доказ) — такий модуль може
// бути й у безневинному DIY. Реальні скімери часто в АЗС/банкоматах.
#pragma once

// true, якщо BT-ім'я збігається з сигнатурою типового дешевого SPP-модуля.
bool skimmer_is_suspect_name(const char* name);
