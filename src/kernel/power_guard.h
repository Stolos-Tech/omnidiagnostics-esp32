// Поріг напруги живлення для вмикання радіо (WiFi/BT/BLE) — чиста логіка,
// тестована на хості. Сплеск струму TX/сканування може посадити стабілізатор
// плати нижче 3.3В і викликати brownout-reset, якщо вхідна напруга й так
// маргінальна. Причина — GPIO34 міряє напругу ПІСЛЯ зарядного IC (T-Display);
// AMS1117 потребує запасу ~1-1.3В над 3.3В під піковим струмом.
#pragma once

#define POWER_GUARD_MIN_MV 3500

// true — напруга достатня, щоб безпечно вмикати радіо.
bool power_guard_radio_ok(int mv);
