// Per-module керування живленням/пінами (усвідомлює СПІЛЬНІ піни SPI-шини).
// Без апаратного load-switch «вимкнення» = зняти драйв з ВИДІЛЕНИХ пінів модуля + відпустити
// SPI-драйвер (SD unmount), а СПІЛЬНІ SCK/MOSI лишити активними, поки ввімкнено хоч один
// SPI-модуль. Опційно: якщо у профілі задано <module>_pwr GPIO (wired до MOSFET/load-switch),
// firmware жене його HIGH=on / LOW=off — тоді це РЕАЛЬНЕ відключення рейки.
//
// Стан вмикання береться з board_profile: profile_get("nrf24"/"cc1101"/"sd").
// g_hw_safe (глобальний SAFE) має пріоритет — тоді всі піни high-Z і apply — no-op.
#pragma once

void module_power_apply();   // застосувати живлення/піни згідно профілю (усвідомлює спільні піни)
