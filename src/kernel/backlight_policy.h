// Трьохступеневе згасання підсвітки за часом бездіяльності — чиста логіка,
// тестована на хості. Не чіпає dev/сон: то окремий (глибший) механізм.
#pragma once
#include <stdint.h>

#define BACKLIGHT_DIM_MS 20000   // 20с бездіяльності -> приглушити
#define BACKLIGHT_OFF_MS 60000   // 60с бездіяльності -> погасити

// idle_ms — час з останньої фізичної активності кнопок; full_level — поточна
// яскравість користувача (0..255, з Settings). Повертає рівень 0..255.
int backlight_level_for_idle(uint32_t idle_ms, int full_level);
