// Службова консоль через USB-Serial: провізіонінг WiFi без телефона/AP.
// Потрібна, коли SoftAP недоступний (забутий пароль, нестабільність), а плата
// підключена кабелем. Креденшали НЕ хардкодяться в код — лише в NVS через цю команду.
//
// Команди (по рядку, 115200):
//   wifi <ssid>|<password>   — під'єднатись і запам'ятати мережу (| = роздільник,
//                              бо SSID може містити пробіли)
//   nets                     — список збережених мереж
//   ip                       — поточний IP/статус STA
//   help                     — довідка
#pragma once
#include <stdint.h>

void serial_console_loop();  // неблокуючий, викликати щоцикл у loop()
