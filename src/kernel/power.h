// Керування живленням: м'яке вимкнення в deep sleep.
// Прокидання — права рідна кнопка (GPIO35, RTC-capable).
// GPIO0 для wake свідомо не використовуємо: це strapping-пін, утримання
// LOW під час reset може завести плату в download mode.
#pragma once

void power_off();       // блокуюче: гасить панель і йде в deep sleep, звідси не повертаємось
void power_on_wake();   // виклик на початку setup(): якщо прокинулись кнопкою — дочекатись відпускання
