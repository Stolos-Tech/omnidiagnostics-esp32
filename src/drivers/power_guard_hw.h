// Апаратна обгортка над kernel/power_guard: читає реальну напругу і кешує
// останнє значення (для показу на екрані, коли вмикання радіо відхилено).
#pragma once

bool power_guard_ok();       // true — можна вмикати WiFi/BT/BLE зараз
int  power_guard_last_mv();  // останнє виміряне значення (мВ)
