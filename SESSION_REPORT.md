# Звіт сесії ESP32-OS — 2026-09-06/07

Повний перелік зробленого за сесію: залізо (nRF), додаток, сервер, автономний QA.

---

## 1. nRF24 — оживлення радіо (залізо)

**Було:** SPI глухо мовчав (усі регістри `0x00`), 2.4G-сканер мертвий.

**Діагностика (покроково, за сигнатурами readback):**
- `0x00` суцільний = MISO не доносить / обрив;
- `0xC5/0xC6` однаково по всіх регістрах, що скаче в `0x00` = живий модуль + **плаваючий контакт** (не тактова);
- чисті reset-дефолти = шина ціла.

**Рецепт розв'язку (порядок важливий):**
1. MISO перенесено **GPIO38 → 21** (38 мертвий на цій платі — підтверджено).
2. Тактова SPI зроблена **рантайм-змінною** (`nrf24_set_spi_hz`, `?hz=`).
3. **Окрема пряма земля nRF в GND ESP** (не через спільний розгалужувач із землею фенів/ULN2003 — ground-bounce від PWM давав плаваючий readback).
4. Заміна 4 сигнальних джамперів (MISO·G21 / CSN·G25 / **SCK·G2 / MOSI·G15** — останні два були фінальні винуватці).

**Результат:** `alive 5/5`, чисті reset-регістри, `write RF_CH=0x25 → readback 0x25`.

**Калібрування частоти:** свіп 1/2/4/6/8/10 МГц після фіксу джгута = **12/12 на ВСІХ** (стеля datasheet 10 МГц). Ранній «дзвін» був суто поганим джгутом. Базову виставлено **8 МГц** (margin під стелею); 10 доступна через `?hz=`.

## 2. Локалізація додатка (v0.70)

Повний аудит `L("…")` vs `Strings.kt` (скрипти `scratchpad/loc_*.py`):
- **9 мертвих ключів** видалено (apps/archive/device/help_sub/language/library/menu/scripts_sub/tools);
- **баг-дублікат ключа `modules`** — секція drawer показувала коротке «MODULES» замість «MODULES & PINS» (mapOf last-wins); HUD-картці дано окремий `modules_hud`;
- закрито реальні одномовні прогалини: підказка IsoBoard, VSWR-зони SmithChart (ЗБІГ/ДОБРЕ/…→MATCH/GOOD/…), **описи модулів ESP/UNO на діаграмах плат** (хелпер `ml(u,e)`);
- EN=UK=55 ключів, нуль missing/розсинхрону. BoardData/SmithChart/ToolHelp — вже двомовні (false-positive regex).

## 3. Калібрування таймінгів (`docs/TIMINGS.md`)

- **nRF dwell — живий свіп:** 150µs=8ch, **300µs=21.7ch@305мс (оптимум)**, ≥500µs — гірше+повільніше. Довший dwell не допомагає (RPD миттєвий, не інтегратор). Дефолт 300µs підтверджено, додано `?dwell=`.
- **Поллінг додатка + rate-limit** (airscan 7с throttle vs live 12с) — узгоджені, змін не потребували.

## 4. OTA-деплой v0.70

Бампнуто 0.69→0.70, зібрано release, задеплоєно на `stolos` (`/opt/espos/ota/`): бекап `.bak69` → scp → cp → manifest (sha з фактичного файлу) → підпис Ed25519 → верифіковано публічними ендпоінтами (manifest v0.70, sha `ddfc44`, відданий APK збігається).

## 5. Автономний QA-луп (бекенд сервера)

**Принцип (вимога юзера):** тестувати РЕАЛЬНИМИ даними з плати (COM5/WiFi HTTP), не моками. Периметр безпечний — локальні копії + тимчасова БД; живий `stolos`/роутер не чіпано, плату силою не ребутано.

**Тести: 49/49 (28 харнес + 21 легасі), pass-rate 1.0**, `board_available: true`.

**3 реальні баги пайплайну знайдено й виправлено:**
| # | Баг | Фікс |
|---|---|---|
| 1 | `ingest_airscan` інжестив 0/28 (плата шле `aps` як dict, код чекав list) | приймає dict+list |
| 2 | Гонка `upsert_*` (SELECT-then-INSERT) → UNIQUE IntegrityError крос-процесно | атомарний `ON CONFLICT DO UPDATE` |
| 3 | non-str MAC у пошкодженому кадрі валив ingest+collector | `str().strip().upper()` + валідація |

**Перевірено без багів:** boot-loop prevention (`boot_state.cpp` — heavy-апки не авто-відновлюються, crash≥2→safe mode), devintel (живий скан .41), App API `/lib/*` (auth 401 / rate-limit 429 / таргет-перемикач), offline-стійкість.

**Бенчмарк:** 111.7 ops/s, пік RAM 1.6КБ (нуль витоку), 8 потоків 0 помилок, запити по індексах.

**Артефакти:** `OVERNIGHT_STABILITY_REPORT.md`, `STABILITY_TEST_RUNS.json`, харнес `server/tests/`.

---

## Файли — змінено / створено

**Прошивка:** `src/drivers/nrf24.{h,cpp}` (MISO 21, рантайм hz/dwell, begin_diag), `src/remote/transport_wifi.cpp` (spitest `?hz=`, nrf/spectrum `?dwell=`/`?diag=`).

**Додаток:** `mobile/.../ui/Strings.kt`, `MainActivity.kt`, `SmithChart.kt`, `ui/IsoBoard.kt`, `build.gradle.kts` (0.70).

**Сервер:** `server/profiles.py` (атомарні upsert, dual airscan, валідація), `server/netlog.py` (захист MAC).

**Нове:** `server/tests/` (board_client, conftest, 5 тест-файлів), `docs/TIMINGS.md`, `OVERNIGHT_STABILITY_REPORT.md`, `STABILITY_TEST_RUNS.json`, `SESSION_REPORT.md`.

## Безпека дотримано

Живий `stolos` і роутер не чіпано (ризик залочення); плату силою не ребутано; Telegram-токен не світив; OTA з бекапом + верифікацією підпису. «Плата не віддає стан» виявилось старою версією додатка — не баг.

## Незакрите / далі

- Прошивку C++ і Kotlin-парсери цей Python-харнес не юніт-тестує (boot-loop перевірено статично, решту наскрізно через реальні відповіді плати) — за потреби окремий firmware-харнес.
- `db.py` connection-per-op коректний, але не high-throughput — батч-транзакції за потреби масового інжесту (зараз не вузьке місце).
- Залізо на відкладеному: SD-модуль (контакт джамперів), фени (живлення 5V напряму).
